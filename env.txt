# Copy and rename to `.env`, modify values

# API

PORTFOLIO_PORT=

# Database

# Docker Compose DB
PORTFOLIO_DB_EXPOSED_PORT=
PORTFOLIO_DB_ROOT_PASSWORD=
PORTFOLIO_DB_NAME=
PORTFOLIO_DB_USER=
PORTFOLIO_DB_PASSWORD=
# Prisma JS
PORTFOLIO_DB_URL="mysql://${PORTFOLIO_DB_USER}:${PORTFOLIO_DB_PASSWORD}@localhost:${PORTFOLIO_DB_EXPOSED_PORT}/${PORTFOLIO_DB_NAME}"
