# Copy and rename to `.env`, modify values

# API

PORTFOLIO_PORT=

# Authentication

# Secret: for example require("crypto").randomBytes(64).toString('hex')
# JWT Expiration: [ms]
PORTFOLIO_BEARER_TOKEN_SECRET=
PORTFOLIO_BEARER_TOKEN_EXPIRES_IN=
PORTFOLIO_REFRESH_TOKEN_SECRET=
PORTFOLIO_REFRESH_TOKEN_EXPIRES_IN=

# Database

# Docker Compose DB
PORTFOLIO_DB_EXPOSED_PORT=
PORTFOLIO_DB_ROOT_PASSWORD=
PORTFOLIO_DB_NAME=
PORTFOLIO_DB_USER=
PORTFOLIO_DB_PASSWORD=
# Prisma JS
PORTFOLIO_DB_URL="mysql://${PORTFOLIO_DB_USER}:${PORTFOLIO_DB_PASSWORD}@localhost:${PORTFOLIO_DB_EXPOSED_PORT}/${PORTFOLIO_DB_NAME}"
